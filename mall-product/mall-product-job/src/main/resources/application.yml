
server:
  port: 9001

spring:
  application:
    name: product-service-job

mall:
  fastjson:
    safe-mode: true

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: delFlag
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath:mapper/*.xml

#seata:
#  application-id: ${spring.application.name}
#  enable-auto-data-source-proxy: false
#  service:
#    grouplist:
#      seata-server: 127.0.0.1:8091
#    vgroup-mapping:
#      my-tx-group: seata-server
#  tx-service-group: my-tx-group
#  use-jdk-proxy: true

xxl-job:
  accessToken: zheshiyigeyongyudiaoduzhongxinhezhixingqitongxindeanhao
  admin:
    addresses: http://${MY_IP:localhost}:9090/xxl-job-admin
  executor:
    appname: xxl-job-executor-product
    ip: ${VM_IP:localhost}
    port: 19999
    logpath: /data/applogs/xxljob/jobhandler/product
    logretentiondays: 30
