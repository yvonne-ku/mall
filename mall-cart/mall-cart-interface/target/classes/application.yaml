
server:
  port: 8004

spring:
  profiles:
    active: dev
  application:
    name: cart-service
#  cloud:
#    sentinel:
#      transport:
#        dashboard: localhost:8686
#        port: 8719

mall:
  fastjson:
    safe-mode: true
  cart:
    max-product: 500

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: delFlag
      logic-delete-value: 1
      logic-not-delete-value: 0
#
#seata:
#  application-id: ${spring.application.name}
#enable-auto-data-source-proxy: false # 重点：必须关闭数据源自动代理，防止和 shardingsphere 的冲突
#  service:
#    grouplist:
#      seata-server: 127.0.0.1:8091
#    vgroup-mapping:
#      my-tx-group: seata-server
#  tx-service-group: my-tx-group
#  use-jdk-proxy: true