-- PROD

-- 基础数据数据库
CREATE
DATABASE IF NOT EXISTS mall_basic_data;

-- 购物车数据库
CREATE
DATABASE IF NOT EXISTS mall_cart;

-- 用户数据库
CREATE
DATABASE IF NOT EXISTS mall_customer;

-- 消息数据库
CREATE
DATABASE IF NOT EXISTS mall_message;

-- 订单数据库
CREATE
DATABASE IF NOT EXISTS mall_order;

-- 支付数据库
CREATE
DATABASE IF NOT EXISTS mall_pay;

-- 商品数据库
CREATE
DATABASE IF NOT EXISTS mall_product;

-- BFF聚合层数据库
CREATE
DATABASE IF NOT EXISTS mall_bff;

-- DEV

-- 基础数据数据库
CREATE
DATABASE IF NOT EXISTS mall_basic_data_dev;

-- 购物车数据库
CREATE
DATABASE IF NOT EXISTS mall_cart_dev;

-- 用户数据库
CREATE
DATABASE IF NOT EXISTS mall_customer_dev;

-- 消息数据库
CREATE
DATABASE IF NOT EXISTS mall_message_dev;

-- 订单数据库
CREATE
DATABASE IF NOT EXISTS mall_order_dev;

-- 支付数据库
CREATE
DATABASE IF NOT EXISTS mall_pay_dev;

-- 商品数据库
CREATE
DATABASE IF NOT EXISTS mall_product_dev;

-- BFF聚合层数据库
CREATE
DATABASE IF NOT EXISTS mall_bff_dev;